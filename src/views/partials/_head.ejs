<!DOCTYPE html>
<html lang="pt-PT">
    <head>
        <!--
            Partial _head.ejs
            -----------------
            OBJETIVO:
            - Definir a estrutura base do <head>:
                - charset
                - viewport
                - título dinâmico
                - CSS (Bootstrap + estilos personalizados)

            NOTA:
            - O título vem de `tituloPagina` (enviado pelo controlador).
              Se não vier nada, usamos um texto por omissão.
        -->
        <meta charset="UTF-8" />
        <meta
            name="viewport"
            content="width=device-width, initial-scale=1, shrink-to-fit=no"
        />

        <title>
            <%= (typeof tituloPagina !== 'undefined' && tituloPagina)
                ? (tituloPagina + ' · Conspira')
                : 'Conspira · Teorias da Conspiração' %>
        </title>

        <meta
            name="description"
            content="Conspira é o hub pastel para explorar, criar e gerir teorias da conspiração em Express + EJS."
        />

        <!-- Tipografia (mesma vibe da app React) -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap"
            rel="stylesheet"
        />

        <!-- Bootstrap 5 (CSS) -->
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
            crossorigin="anonymous"
        />

        <!-- Estilos personalizados (public/style.css → /style.css) -->
        <link rel="stylesheet" href="/style.css" />

        <!-- Phosphor Icons -->
        <script
            src="https://unpkg.com/@phosphor-icons/web"
            defer
            crossorigin="anonymous"
        ></script>
    </head>

    <!--
        O body é aberto aqui e fechado no partial _footer.ejs.
        A classe `conspira-body` é usada pelo tema pastel definido em style.css.
    -->
    <body class="conspira-body d-flex flex-column min-vh-100">
