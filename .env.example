# .env.example
# -------------
# Exemplo de variáveis de ambiente para a Ficha 5.
# Cada aluno deve copiar este ficheiro para ".env" e preencher os seus dados.
#
# Para copiar:
#   No terminal:  cp .env.example .env
#
# Depois edita o .env com os valores certos.

# Connection string do MongoDB Atlas.
# Exemplo de formato:
# mongodb+srv://UTILIZADOR:PASSWORD@nome-cluster.abcde.mongodb.net
MONGODB_URI="PASTE_A_TUA_CONNECTION_STRING_ATLAS_AQUI"

# Nome da base de dados a usar dentro do cluster MongoDB.
# Podes manter este nome igual para toda a turma.
MONGODB_DB_NAME="ficha5_conspiracoes"

# Segredo usado para assinar as sessões (cookies de sessão).
# IMPORTANTE: cada aluno deve inventar uma frase longa e difícil de adivinhar.
SESSION_SECRET="troca_esta_frase_por_uma_coisa_grande_e_dificil"

# Porta onde o servidor vai correr em desenvolvimento.
# Se não definires, o código usa 3000 por omissão.
PORT=3000
